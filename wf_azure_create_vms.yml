- name: Create Azure VM
  hosts: localhost
  connection: local
  vars:

  tasks:
  - include_role:
      name: ansible-role-azure-vm

  - name: make public ip var available for other playbooks
    set_stats:
      "{{ azure_vm_vm_prefix }}-public_ips": "{{ public_ips }}"
